import { PersonListsPanel } from "../components/PersonDetailPanels";

/**
 * Renders the lists tab for one contact detail page.
 * @returns {JSX.Element}
 */
export default function PersonListsRoute() {
  return <PersonListsPanel />;
}
