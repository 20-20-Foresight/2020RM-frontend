import { PersonNotesPanel } from "../components/PersonDetailPanels";

/**
 * Renders the notes tab for one contact detail page.
 * @returns {JSX.Element}
 */
export default function PersonNotesRoute() {
  return <PersonNotesPanel />;
}
